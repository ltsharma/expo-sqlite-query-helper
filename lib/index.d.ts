import { Databse, executeSql, insert, update, search, createTable, dropTable, deleteData } from './database';
export default Databse;
export { executeSql, insert, update, search, createTable, dropTable, deleteData };
